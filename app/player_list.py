from app.player_node import PlayerNode
from app.player import Player


class PlayerList(object):
    def __init__(self, player, uid):
        self.__head = None
        self.__tail = None
        self.list_length = 0

        key = uid


    def is_empty(self):
        return self.__head is None

    def find(self):
        current_player = self.__head

        while current_player and

    def insert_item(self, key):
        new_player = PlayerNode()
        if self.__head == None:


    # def delete(key):

    # def delete_head()
